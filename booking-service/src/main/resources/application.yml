spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bookingdb
    username: jugger
    password: passw0rd
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true
  jackson:
    serialization: {}
server:
  port: 8082
